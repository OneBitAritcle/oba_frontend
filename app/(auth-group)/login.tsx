import { View, Text, Button } from "react-native";

export default function Login() {
  return (
    <View style={{ padding: 20 }}>
      <Text>로그인 페이지</Text>
      <Button title="Google 로그인" onPress={() => {}} />
    </View>
  );
}
